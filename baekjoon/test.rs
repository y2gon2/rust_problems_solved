use std::collections::HashMap;
use std::cmp::max;

fn main() {
    println!("{:?}", most_common_word("Bob hit a ball, the hit BALL flew far after it was hit.".to_string(), vec!["hit".to_string()]));

}
pub fn most_common_word(paragraph: String, banned: Vec<String>) -> String {
 
    return "aa".to_string();
}